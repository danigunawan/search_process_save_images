<h3>The word "<%= @search_word %>" has been highlighted in the following image: </h3>

The image, originally named "<%= @image.associated_image_file_name %>" and uploaded on <%= @image.created_at.strftime("%B %d, %Y") %>, can be found in greater detail <%= link_to 'here', image_path(@image) %>.

</br></br>
You can also submit another search:
</br>

<%= render partial: "search_form" %>

<%= image_tag image_search_image_png_path(search_word: @search_word) %> 

<%= link_to 'All Images', images_path %>